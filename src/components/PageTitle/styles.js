import styled, { keyframes } from 'styled-components';

export const PageTitleContainer = styled.div`
  display: flex;
  align-items: center;

  h1 {
    color: #7a7a7a;
    margin-right: 2rem;
  }
`;
